import { mkdirSync, readFileSync, writeFileSync } from "node:fs";
import { resolve } from "node:path";
import solc from "solc";
const root = resolve(import.meta.dirname, "..");
const workspace = resolve(root, "../..");
function findImports(path) {
  for (const candidate of [path, path.replace(/^\.\//, "src/")]) {
    try {
      return { contents: readFileSync(resolve(root, candidate), "utf8") };
    } catch {}
  }
  return { error: `Import not found: ${path}` };
}
const entry = "src/BNBXHolderRewardsFactory.sol";
const input = {
  language: "Solidity",
  sources: { [entry]: { content: readFileSync(resolve(root, entry), "utf8") } },
  settings: {
    optimizer: { enabled: true, runs: 200 },
    evmVersion: "shanghai",
    outputSelection: {
      "*": {
        "*": ["abi", "evm.bytecode.object", "evm.deployedBytecode.object"],
      },
    },
  },
};
const output = JSON.parse(
  solc.compile(JSON.stringify(input), { import: findImports }),
);
const errors = (output.errors ?? []).filter(
  (item) => item.severity === "error",
);
if (errors.length)
  throw new Error(errors.map((item) => item.formattedMessage).join("\n"));
const factory = output.contracts[entry].BNBXHolderRewardsFactory;
const token =
  output.contracts["src/BNBXHolderRewardsToken.sol"].BNBXHolderRewardsToken;
const runtimeBytes = factory.evm.deployedBytecode.object.length / 2;
if (runtimeBytes > 24_576)
  throw new Error(`Factory runtime exceeds EIP-170: ${runtimeBytes}`);
const lib = resolve(workspace, "apps/web/lib");
mkdirSync(lib, { recursive: true });
writeFileSync(
  resolve(lib, "holder-rewards-factory-deployment.ts"),
  `// Generated by packages/contracts/scripts/export-holder-rewards-web-artifact.mjs.\n` +
    `// Independent holder-rewards Factory artifact; do not edit by hand.\n` +
    `export const holderRewardsFactoryAbi = ${JSON.stringify(factory.abi)} as const;\n` +
    `export const holderRewardsFactoryBytecode = "0x${factory.evm.bytecode.object}" as const;\n`,
);
writeFileSync(
  resolve(lib, "holder-rewards-token-creation-bytecode.ts"),
  `// Generated by packages/contracts/scripts/export-holder-rewards-web-artifact.mjs.\n` +
    `export const holderRewardsTokenAbi = ${JSON.stringify(token.abi)} as const;\n` +
    `export const holderRewardsTokenCreationBytecode = "0x${token.evm.bytecode.object}" as const;\n`,
);
console.log(`Holder-rewards Factory runtime: ${runtimeBytes} bytes`);
